'use strict';

/**
 * sessions-wrapper — tools/sessions_spawn.js의 re-export
 * comment-monitor.js 등에서 사용
 */
const { sessions_spawn } = require('../../tools/sessions_spawn');

module.exports = { sessions_spawn };
